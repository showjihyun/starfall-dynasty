# STARFALL 슬라이스 현황

| 슬라이스 | 유형 | 상태 | 마지막 Phase | 최신 QA | 갱신일 |
|---------|------|------|-------------|---------|-------|
| p0-01-bootstrap | BOOT | done | 6 | r1 PASS 37/37 (기록 1) | 2026-09-18 |
| p0-02-networking-spike | SLICE | done | 6 | r1 PASS 73 / 부분 1 / FAIL 0 | 2026-09-19 |
| p1-01-ship-movement | SLICE | evaluating | 5 | r4: 블록 9 전원 PASS · SC-59 **FAIL(증거 요건)** · 위반 원인 확정 — 블록 6·7·8 잔여 | 2026-09-23 |

## Phase 0 — 종료 기준 충족 (2026-09-19)
"30명 동시 접속 + 실시간 이벤트 기록" 실증 완료: 31 연결(봇 30 + Unity 1), 명령 3,570건 손실 0, 마지막 봇 종료 후 0.242초에 전 행 DB 가시, DB 30초 중단에도 유실 0.
성능 기준선(p1 회귀 비교용): tick 초과 0.000%, 단일 tick 본문 최대 21.65ms, 왕복 p99 약 50.4ms.

## p1-01 진행 (2026-09-20)
- Phase 2~3 완료: 스펙·ADR-0010/0011·계약 7타입, 스프린트 계약 SC-01~86(508줄, 구현자 확인 10쟁점 반영 완료).
- Phase 4 완료: server 단독 실행 152 passed / 0 failed(fmt·clippy 0), client EditMode 141 tests / 0 failed.
  - server S10(tick당 세션별 명령 상한 8)으로 p0-02 이래의 `sc20` 간헐 실패를 **튜닝이 아니라 설계로** 제거.
  - QA 평가 도구 확장 완료: 봇 하네스 49 테스트(고장 주입 9종 전부 검출), e2e 스크립트 4종 자체 검증 통과.
- Phase 5 진행 중.
  - **QA r1 (블록 0·1·2 + SC-82·83·84·86) = 37/86 판정: PASS 34 / FAIL 3 / 미검증(환경) 0.** 리포트 `04_qa_report_r1.md`(618행), 증거 `evidence/`(30개 로그).
    - 커버리지 errors **14 → 0**. 경계면 **201행 대조 / 불일치 0**.
    - `cargo test --workspace --locked` 3회 전부 통과 — p0-02 이래 5회 중 2회 실패하던 `sc20`의 간헐 실패가 S10 설계로 사라진 것이 독립 확인됐다.
    - QA가 자기 도구의 거짓 경보를 잡았다: 경계면 비교 첫 실행의 불일치 39건이 전부 "널 가능 vs 선택"을 구분 못 한 비교 규칙 탓. 총계만 믿었으면 server에게 헛된 요청 39건을 보냈을 것 — 계약의 "총계가 아니라 표가 근거다"가 실제로 작동했다.
  - **FAIL 3건은 전부 동작 결함이 아니라 검증 부재였다** (server가 TDD로 확인: 요청된 단언이 전부 첫 실행에 통과).
    - SC-18(server) → soft/hard 분리 + "경계에서도 조작은 먹는다"를 `v=(-1.25,0,1.75)` 한 벡터로 증명. **닫힘**
    - SC-19(server) → 이월 플래그 `[1×10, 0×5]` + 만료 후 tick당 0.35 감쇠 곡선. M-17의 "이월"·"이월 만료" 두 줄도 이것으로 채워진다. **닫힘**
    - SC-46(client) → EditMode exit 8. 실패 테스트는 0건이고 `Inconclusive` 1건이 원인. client가 심은 pending 신호가 server의 S6 산출물 도착(06:12)으로 켜졌는데 client 실행(05:27)이 더 일러 아무도 못 봤다. **client 작업 중**
  - server 최종: `cargo test --workspace --locked` 3회 연속 **154 passed / 0 failed**(기존 152 + 신규 2), fmt·clippy 0.
- 블록 3~9 미착수. 블록 6(Unity 실서버·예측·육안)은 client 실행이 필요하다.

### 리더 결정
- **G-a: `docker compose down -v`를 쓰지 않는다** (2026-09-20). SC-80은 이번 실행의 tick 구간 한정으로 판정한다.
  근거: ① p0-02의 `QA_APPEND_ONLY_PROBE` 행이 슬라이스를 넘어 살아 있다는 것이 절대 원칙 5의 살아있는 증거다 ② SC-80이 묻는 것은 "이번 슬라이스 서버가 위치 시계열을 쓰는가"이므로 tick 구간 한정이 질문에 맞는 범위다 ③ 되돌릴 수 없는 삭제를 한 항목의 범위를 넓히자고 할 값이 아니다.

### 라운드 4 후반 (2026-09-23) — 블록 9 전원 PASS · SC-59 FAIL · 위반 원인 확정

**블록 9 — 10항목 전부 PASS.** 핵심은 **SC-81 전수 등식 성립**: `domain_events` 4,533행에서 인과 결함이 **정확히 7건 = 동결 장부**, 신규 0 · 누락 0(ship_events 168 · session_events 4,364 · superseded 2). 도구 양방향 자기 검증(전수 7 검출 / 블록5 구간 0 / 알려진 위반 구간 exit 1)과 합성 17행 자체 검사 10종 전부 검출. SC-79는 전수 4,533행 / 1,735그룹 위반 0. **구간 표본이 작다**(함선 1척·이벤트 6행) — 블록 6~8이 큰 구간을 만들면 SC-76·78·80 재실행 필요.
- SC-78이 **사람 조종 실플레이의 재개**를 처음 관측했다(sessions 2 / spawns 1).
- server 트리 변경(`45970d72…` → `477c9c54…`)으로 SC-01·85·87 재실행 → 전부 PASS, qa2와 수치 동일. **바이너리 재빌드**(`d5e47ed7…` → `e84cd098…`) — 디스크 바이너리가 소스와 어긋나 있었고 **어제 SC-59 세션은 옛 바이너리로 돌았다.**
- 계약 외 기록: 짝 없는 함선은 **3척이 아니라 4척**(이전 리포트가 하나를 빠뜨렸다). 넷 다 오래된 행, 신규 없음. 지우지 않는다(원칙 5).

**SC-59 — FAIL(증거 요건). 부호 버그가 아니다.** 사람이 관찰한 것은 전부 기대와 일치했고 부호가 뒤집혔다는 증거는 없다. FAIL 사유: ① (a)의 **마우스 선회·위 추력 관찰이 없다**(리더가 절차를 W만으로 줄였다) ② (b)는 마커 4개 중 1개만 ③ **클립 a 순간 Game view에 HUD가 한 줄도 없어** 계약이 스스로 적은 실격 조건("화면에 입력 상태가 없으면 그 영상은 부호를 증명하지 못한다")에 걸린다. **5~10분 재촬영 필요.** §0.10 문장은 계속 유효하다.

**⚠ 리더 기록 오류 3건 — 정정 기록 `evidence/R4-B6/sc59/08-correction.md`**(원 기록은 고치지 않고 표시만 달았다):
1. 색인 프레임 6장이 HUD가 아니라 **전부 터미널 창**이었다. 리더가 **한 장도 열어보지 않고** "좌상단이니 들어간다"는 **추론을 관측처럼 적었다** — 이 슬라이스가 내내 붙잡은 오류(§7a)를 증거 문서에서 저질렀다.
2. "평범하게 비행하다 끊겼다"가 **관측과 다르다**. 위반 전후 다섯 프레임 전부 Unity가 전경이 아니었다(터미널·Chrome). **"Editor가 백그라운드인 동안"**이 맞고, 이는 client 진단과 **오히려 더 잘 맞는다**(백그라운드 → 프레임 지연 → catch-up 버스트).
3. 재조정 수치가 기록보다 나쁘다. **카운터가 재접속에서 초기화된다** → 세션 전체 hard snap **최소 8건**(M-9 목표 0), 위치 오차 최댓값 56 m가 아니라 **최소 323 m**.

**🔴 위반 원인 확정** (client 진단 → qa3가 코드로 교차 확인): `GreyboxSession.cs:418~431`의 catch-up 루프에 **프레임당 드레인 상한이 없다.** 0.46초 히치 하나로 한 프레임에 **9건** 송신 → 서버 tick 상한 8 초과 → 위반. **서버 설계 사안이 아니다**(상한 8의 전제가 옳고 클라이언트가 그 전제를 깼다).
- **qa3가 찾은 구분 — 어긋남 경로가 둘이고 방향이 반대다**(`:433~452`): **A** 서버가 버림 → 클라이언트만 예측 반영 → hard snap. **B** `outbound queue full` → `TrySendJson` false → `return` → 양쪽 다 미반영 → **플레이어 입력이 조용히 사라진다.** 녹화 상태 바에서 관측된 `outbound queue full (256)`은 **B**다.
- **리더 지적**: B의 `return`은 그 tick의 **예측 전진 자체를 건너뛴다**. 서버는 이월로 전진하므로 **tick 수가 어긋난다** — A·B 모두 "못 보낸 tick도 서버의 이월을 그대로 예측"해야 한다.
- **1002가 보이지 않는다**: 서버는 `ProtocolViolation`을 close **1002**로 닫고 1002는 재연결 대상이라, 클라이언트가 `PROTOCOL_VIOLATION`을 **한 번도 로그에 남기지 않는다** — DB `close_reason`과 클라이언트 로그가 서로를 가리키지 못한다.
- **리더 승인(2건)**: 송신 건수 계측 + 1002 전용 grep 태그. 없으면 SC-89가 **"끊겼는가"만 보는 검사**가 되어 "고쳐져서 안 끊긴 것"과 "운이 좋아 안 끊긴 것"을 구분하지 못한다(§7a). **재연결 동작은 바꾸지 않는다**(4001과 달라야 한다).
- **열린 것**: `reconcile_error_deg max`가 재접속 전후 동일(80.5663) — 초기화 안 되거나 포화. **지표를 확인하기 전 SC-56 판정 불가.** 그리고 **SC-56 재측정은 catch-up 수정 뒤에** — 수정 전 수치는 "재조정이 나쁘다"가 아니라 **"송신이 버스트였다"**를 잰 것이다.
- **architect 대기 5건**: 수정 형태(K·이월 흉내·경로 B 포함 여부), SC-89 신설 승인, SC-59(b) "마커 4개" 문구, 지표 신뢰성, 도구 라벨.

### SC-59 부호 육안 검증 — **완료** (2026-09-23, 사용자가 직접 조종·관찰)
에이전트 주간 한도(9/26 해제)로 qa2·client 불가 → **리더가 환경을 세우고 사용자가 관찰.** 판정은 qa2 몫, 기록은 `evidence/R4-B6/sc59/`.

- **부호가 뒤집혔다는 증거는 관찰되지 않았다.** 클립 a(W → 뱃머리 방향 이동) · b(마커 대비 이동, `origin_distance_m` 변화) · c(soft에서 `BOUNDARY WARNING`, hard에서 튕김 없이 미끄러짐, **그 상태에서도 조작이 먹음**) · d(손 떼면 수평 복귀 / Q·E 누른 채면 복귀 안 함) 전부 기대와 일치.
- **이 항목만이 부호를 검출할 수 있다**(§0.10): 서버·클라이언트가 같은 공식을 쓰므로 자동 검증은 부호가 뒤집혀도 전부 통과한다. 라운드 1~4의 모든 PASS가 이 사실과 양립했다.
- server SC-18의 `v = (-1.25, 0, 1.75)`가 **"플레이어가 경계에 갇히지 않는다"**를 뜻함을 처음 확인(사람이 경계에서 선회·복귀).
- 최고속 실측 `speed_mps = 135.8`(설계 140) → 가속·최고속은 설계대로. 사용자 체감 "조금 천천히"는 **공간 스케일** 신호(designer 입력: 마커 배치 또는 `max_speed_mps`).

#### 🔴 실서버 발견 2건 (qa2·client·server 추적 필요)
1. **정상 플레이 중 `PROTOCOL_VIOLATION`으로 강제 종료됐다.** 약 7.6분 비행 뒤 서버가 끊음 — 로그: `프로토콜 위반 예산 초과 budget=8 window_s=10`, `dropped=0`. tick당 명령 상한(8) 초과가 10초 안에 8번 쌓였다는 뜻. **봇은 송신 속도를 통제해 보내므로 이 경로를 한 번도 밟은 적이 없다** — 라운드 1~4의 모든 PASS와 양립(§7a 형태). 계약에 "정상 플레이 중 끊기지 않는다" 항목이 없다. **임계를 늘려 닫지 말 것**(S10이 막으려던 것이 다시 열린다).
2. **재조정 이상치**: `reconcile_hard_snap_total` 1 → **5**(M-9 목표 0), `reconcile_error_m` max **56.3 m**(p99 0.0012), `reconcile_error_deg` max **80.6°**(p99 1.1), n=1013. p50·p99는 거의 0인데 최댓값만 튄다. hard snap은 위반보다 **먼저** 관측돼 "끊김이 원인"은 아니며, **둘이 같은 상류 원인(명령 버스트)을 공유**할 가능성. 00_request 3번("예측·보정")에 직접 걸린다.

#### 부수 — 실플레이 최초 관측 (qa2가 해당 SC에 쓸 수 있다)
끊김 뒤 재접속이 **스폰 없이 같은 함선으로 재개**(SC-11 계열) · 정상 종료 후 **정확히 600 tick** 뒤 디스폰(SC-32) · `SHIP_SPAWNED` 1건뿐(I-29) · `RATE_LIMITED = 19`가 실서버에서 계수됨(R3 §4.5 해소 확인) · `cl2_reconcile_has_error_total` 77 → **1013**(SC-56 관찰 ①이 0이 아님).

### 사용자 결정 5·6 (2026-09-22) → 라운드 4
- **결정 5 — 동시 접속은 (B) 나중 접속이 이어받는다.** `SUPERSEDED` 닫기 + close 4001 + 클라이언트는 4001이면 재접속 안 함. 상세: `00_request.md`.
- **결정 6 — 라운드 4 승인, 블록 6~9 전부.** 사용자 참여 2회(SC-59 육안, 블록 8 Unity PlayMode).
- architect R3 추가 판정: 자기 참조는 **세 층으로** 막는다 — 구조적 도달 불가 / 매 tick 1:1 검사 + `debug_assert` / 릴리스에서 도달하면 **원인을 지어내지 않고** 이벤트를 쓰지 않은 채 ERROR("빠진 기록은 짝 검사가 잡지만 그럴듯한 가짜 원인은 아무도 못 잡는다"). DB의 자기 참조 7건은 지우지도 고치지도 않고 **동결된 결함 장부**로 판정(SC-81). SC-11(3)은 와이어 층 = 양자화 봉투 + 음성 대조, 비트 일치는 f64 층(AC-3(d1))으로 분리. SV-1은 설계·K=10 유지, **전제를 교체**("부하는 폴 비용으로는 폴 수를 못 올리지만 서버 종료 태스크를 굶겨 실제 종료를 늦추면 올릴 수 있다").
- **"SC 기준 열린 FAIL 0"을 완료로 읽지 않는다** — I-29는 계약 밖이지만 슬라이스 종료를 막는 결함이다(architect).
- **라운드 4 구현 완료(2026-09-22)**:
  - architect: 계약에 `SUPERSEDED` + 유효 fixture 1건. 실측 스키마 18 / **유효 27** / 반례 34 / 레지스트리 13 — 예측대로 유효만 변화. **역방향 대조**(새 fixture를 옛 스키마로 검증하면 거부 = 새 값을 실제로 검사한다). Rust 계약 테스트 2건 RED가 "fixture가 양쪽 열거를 묶는다"의 증거.
  - server: S-1(재현 RED) → S-2(`SessionState.controlling_ship` — 세션이 자기 함선을 스스로 기록, 자기 참조 폴백 삭제) → S-3(`debug_assert_i29`로 sim 기존 45개 테스트가 전부 1:1 검사를 공짜로 통과) → S-4(재개 f64 비트 일치) → S-5(SV-1 전제 정정, "참 양성" 철회) → S-6(넘겨받기 + 4001). **167 passed / 0 failed ×3**, golden 불변.
  - client: **4001이 실제로 읽힘을 실소켓으로 확인**(RFC 6455 핸드셰이크를 손으로 완성한 로컬 서버가 진짜 Close 프레임 전송) — (B) 정책의 전제. 4001이면 재접속 안 함 + 다른 코드 6종은 여전히 재접속(양성 대조). 실측 27 발견 / 21 왕복. EditMode **159 / 157 / 0 / inconclusive 0**.
  - **운영 주의(client 발견)**: 블록 6 SC-59 녹화와 블록 7 2세션 하네스가 같은 identity 해석을 써서, 동시에 띄우면 나중 것이 먼저 것을 넘겨받기로 밀어낸다 → **순차 실행** 또는 identity 분리. 역으로 SC-88(d)를 Unity로 재는 방법이기도 하다.
  - 리더 브리핑 오류 1건(기록): "`unknown-close-reason.json`이 여전히 C#에서 거부되는지"는 틀린 전제였다 — C#은 `close_reason`을 설계상 문자열로 두어 애초에 거부한 적이 없다(§0.5 "감지 불가 10건"). client가 정정.
- 라운드 4 순서: ① architect 계약 변경(`SUPERSEDED`) ∥ server S-1~S-5(정책 무관 구조 수정) ∥ client 4001 처리 ∥ qa 계약 7차 개정 → ② server S-6(넘겨받기) + client C# 재생성 → ③ qa I-29 재판정 + 블록 0 회귀 → ④ **새 qa 세션**으로 블록 6~9.

### QA 라운드 3 (2026-09-21~22, 진행 중 — 계약상 마지막 라운드)
- **S-A·S-B·S-C 셋 다 닫힘**(qa 독립 재현). 실서버 봇 received +199 / rejected 0, `ack_last=Some(328)`, 함선이 실제로 움직인다(path 2615.69 m, speed_max 140.00 m/s). `shutdown exit=0` 3/3.
- **S-B·S-C 원인 = 동기 stdout 로그 + 드레인 안 되는 4096 B 파이프.** 100% 재현(간헐 아님). qa가 **귀속을 스스로 정정**했다: 절반은 qa 하네스(`server_boot.py`가 종료 후에만 읽음), 절반은 서버(로그 소비자가 느리다고 게임 서버 전체가 정지하는 것은 정당하지 않다). 양쪽 다 고침 — server는 `logsink.rs`(비블로킹 유계 큐), qa는 드레인 스레드.
- **qa 도구가 "틀렸는데 초록불"이었다**: 봇 짝 게이트 `accepted == replies_total`이 `accepted = 0`이라 통과 — **명령이 하나도 수락되지 않을 때만 통과하는 검사**였다.
- **architect R3 판정 3건** — ① flaky(`graceful_client_close_is_prompt`)는 **관측 설계 FAIL**: 벽시계가 서버 지연이 아니라 **자기 폴링 비용**을 쟀다. 예산을 늘리지 않고 **폴 횟수 단언**으로(SV-1), 증거는 반복 횟수가 아니라 **부하 재현** ② 명령→응답 관계는 스펙 §5.1a 규범 표로. **레지스트리 `responses` 필드는 미룬다**("두 점으로 스키마를 긋지 않는다" — 세 번째 명령 타입이 들어오는 p1-02+에서 ADR과 함께) ③ 로그 파이프 e2e를 **AC-2(i)** 로 채택(당초 (h)로 신설됐으나 기존 (h)와 글자가 겹쳐 architect가 (i)로 정정), 조건: 파이프가 실제로 찼음을 단언 / RED 선행 / stderr 처리.
- **세 판정을 관통하는 규율**(계약 §7 옆 상설화 예정): **"관찰이 겨냥한 조건이 실제로 발생했는지를 함께 단언하지 않으면 초록불은 아무것도 뜻하지 않는다."** 이 슬라이스의 `world_full`·`SET_SHIP_CONTROL`·봇 게이트·벽시계 flaky가 전부 같은 형태였다.
- **블록 4**: SC-23·66·67 PASS, SC-68(e)(g) PASS / (f) 미실행, SC-24 미관측(qa probe 설계 미비).
- **신규 서버 결함(§4.5)**: `commands_rejected_total`이 tick 층 거부(`STALE_INPUT`·`RATE_LIMITED`)를 세지 않는다 — 봇 수신 40·6건 vs stats 델타 0·0. `record_rejection` 호출이 게이트웨이 한 곳뿐. **SC-33 PASS 불가**.
- **라운드 3 최종(2026-09-22)**: PASS 12(SC-13·14·23·24·31·32·33·66·67·68·76·85) + AC-2(i) PASS / FAIL 1(SV-1 — 귀속 "관측 설계", 수정 확인으로 닫음) / 보류 1(SC-11 — 계약 결함). **SC 항목 기준 열린 FAIL 0.** 라운드 2 FAIL 12건 → 11 PASS + 1 보류.
  - AC-2(i) RED→GREEN: 싱크를 끄면 파이프가 찬 뒤 healthz 0/10·shutdown 무효, 파이프를 읽기 시작한 지 0.06초 만에 exit 0(인과 확인). GREEN은 파이프가 찬 채로 healthz 10/10, 읽지 않은 채 shutdown exit 0. 1300 연결에서 싱크가 4538줄을 버렸다.
  - SC-33: 8라벨 전부 봇 관측과 일치(STALE 40 / RATE 6 / DUPLICATE 1 / MALFORMED 13).
  - SV-1: server의 "참 양성" 해석은 **독립 증거 없는 순환**(폴로 폴을 정당화). 전제 "부하는 폴을 늘리지 않는다"도 문자 그대로 틀림 — 폴은 게이지를 내리는 태스크의 기아에 민감하다. 현실 경합 90회 새 형태 FAIL 0. **단 원래 flaky 조건은 qa도 server도 재현하지 못했다.**
  - **🔴 계약 외 결함 — 같은 actor 동시 세션(I-29·I-30·I-40 위반)**: 둘째 세션이 둘째 함선을 스폰 → 첫 함선이 세션 없는 `ACTIVE` 유령 → 종료 시 **causation이 자기 자신인** `SHIP_DESPAWNED`. 코드 주석은 "있을 수 없다"였다(이 슬라이스의 네 번째 "있을 수 없다고 믿은 경로"). 현실 경로: 소리 없는 끊김 후 `IDLE_TIMEOUT` 30초 안 재접속. **DB에 자기 참조 7건이 영구히 남았다** → 블록 9 SC-81을 막는다. 짝 검사 `pairs`로는 안 잡히고 qa 신규 `overlap`·`causation`이 잡았다. 스펙 I-29에 이 경우가 없어 **정책 결정 필요**(거부 vs 넘겨받기) → architect 판정 중, 사용자 결정 가능성.
  - **블록 6~9 미실행** — 계약상 라운드 예산(3) 소진. Phase 5-3에 따라 사용자 결정 필요.
- 세션 재시작(2026-09-22)으로 팀원 재투입: `server`(SV-1 부하 재현 + §4.5 수정) → 신호 → `qa`(도구 작업은 병렬, 실서버 측정은 server 신호 뒤 — G-c).
- **문서 결함 발견**: 스펙 AC-2에 `(h)`가 두 개 → architect가 로그 파이프 항목을 **(i)** 로 정정(2026-09-22).

### QA 라운드 2 (2026-09-21) — **실서버가 서버 결함 3건을 드러냈다**

재판정 3건(SC-18·19·46) 전부 닫힘. 신규 PASS: SC-51·52(실S6, 아래)·SC-55·SC-87. 블록 3·4·5 실행.
**라운드 2 집계: PASS 16 / FAIL 12 / 보류 3. FAIL 12건 중 11건이 아래 결함 3개가 직접 막은 것이다.**

| # | 결함 | 증거 |
|---|------|------|
| **S-A** | **`SET_SHIP_CONTROL`이 게이트웨이에서 통째로 거부된다.** `gateway/src/ws.rs:579`가 `PING_SERVER`가 아니면 전부 `UNKNOWN_COMMAND_TYPE` | 봇 200건 송신 → 거부 +200, `commands_received_total` 델타 **0**(큐에 넣으려 시도조차 안 됨). 스냅샷 **1,066건 전수 `ack_input_seq = null`**, 봇 4대 전부 `ack_last = None` |
| **S-B** | **세션이 닫히지 않는다.** `수신 태스크 종료`는 찍히나 `세션 종료 제출`이 없다 | OS ESTABLISHED 0인데 `ws_connections=1`, 함선 `ACTIVE` 영구 잔존, CLOSE_WAIT 누적, `IDLE_TIMEOUT=30초`가 3분 넘게 미발동 |
| **S-C** | **서버가 무응답이 되고 `shutdown`이 안 먹는다. 3회 중 2회** | 로그가 세션 수명 경계에서 끊김. `/healthz` 20초 타임아웃에도 000. 프로세스 생존, CPU 누적 0.25초 = 정지. 정상 종료 2/2 실패 → 하드 킬(§0.1 위반이므로 QA가 명시 기록) |

**S-A가 왜 154개 테스트를 통과했는가**: `grep -c "SET_SHIP_CONTROL" gateway/tests/ws_integration.rs` → **0**. 게이트웨이 통합 스위트가 그 명령을 한 번도 보내지 않는다. `world_full`과 같은 계열이지만 훨씬 크다 — **이 슬라이스의 제목이 "함선 이동"이다.**

QA가 §0.10에 더한 문장: **"이 슬라이스는 '함선 이동'인데, 함선을 움직이는 명령이 소켓을 통해 시뮬레이션에 도달한 적이 한 번도 없다. 라운드 1·2의 PASS 50건이 전부 그 사실과 양립한다."** — 실서버 관측을 계약에 넣어 둔 이유가 이것이다.

**가장 깨끗한 관측(PASS)**: SC-10 — 같은 actor가 잔류 만료 후(35.7초 > 30초, G-j 준수) 재스폰됐고 두 위치가 `(0, -250000, 2500000)` mm로 **정수까지 동일**, `data/`의 `points_m[3]`과 일치. 시드 해시가 동작하고 난수가 아니다.

**QA가 자기 판정을 뒤집은 건(기록)**: SC-55를 FAIL → **PASS**로 정정. 근거였던 요구(실S6 각속도 대조)가 architect 판정으로 **구현 이후에 신설된 것**(AC-12(f))이라, 있던 구현을 나중 요구로 떨어뜨리는 건 공정하지 않다고 판단. 뒤집은 사실과 이유를 리포트에 남겼다.

### architect 결정 (R2 판정, 2026-09-21) — SC-51/52 검증 경로
client가 `Reconcile()` 대신 `ShipIntegrator.Step` 직접 대조로 SC-51/52를 구현한 것을 **수용**. 계약 문구대로 돌리면 첫 지점은 1 tick분 히스토리 vs 2 tick분 확인 상태로 **미터급 장부 불일치**, 나머지 ~293지점은 매칭 실패로 `HasError=false` → 오차 **자명하게 0** — 299지점 중 유효 측정이 사실상 없다. client 경로는 **더 엄격하다**(되감지 않고 600 tick 누적 표류. 재조정을 끼우면 2 tick 창만 잰다).
- **architect가 찾은 구멍**: 대조 루프가 각속도를 파싱만 하고 안 썼다 → `angular_velocity_roll_mdeg_s`를 떨어뜨려도 통과했다. **AC-12(f) 신설**(CL-1). client가 닫고 **떨어뜨리면 실제로 FAIL하는 것까지 실측 확인**했다.
- **SC-56도 자명하게 통과하는 상태였다**(`HasError=false`면 p50/p99가 0으로 찍힌다). AC-13(b)에 관찰 ①②③ 추가, **①(HasError=true 호출 수)이 0이면 FAIL**. CL-2 계측은 client가 넣었고 실행은 S-A 수정 후.
- fixture는 **바꾸지 않는다** — 조밀하게 하면 SC-51/52가 오히려 약해지고, golden 재생성이 SC-34 PASS·bless 게이트 검증·스테이징 3파일·client 실측치를 전부 무효화한다.

### architect 결정 (R1 통지 처리, 2026-09-20)
- **문서 옛값**: 정본은 구현과 계약 §0.5. 스펙을 그쪽에 맞췄다(`ShipState` 18필드 / C# 거부 18·통과 10·계층없음 6 / 반례 34). QA 통지 2칸 외에 **2칸이 더 있었고, 그중 하나가 AC-10(c)** 였다 — 수용 기준 안의 옛값은 구현자가 코드로 옮기므로 더 위험했다. §8 U-14는 **날짜 박힌 측정 기록이라 덮어쓰지 않고 주석만** 달았다(문서에도 "과거 기록은 수정하지 않는다"를 적용).
- **S6 재생 산출물: git에 추적한다** (ADR-0010 §3.1 신설). 근거: "같은 비트"의 전제는 두 언어가 같은 절차를 도는 것이 아니라 **같은 파일을 여는** 것이다. 추적하지 않으면 기준이 "마지막으로 로컬에서 `cargo test`를 돌린 사람의 파일"이고, SC-51/52가 초과했을 때 "두 구현이 다르다"와 "두 사람이 다른 파일을 봤다"를 **구분할 수 없다**.
  - (a)의 약점(테스트가 추적본을 조용히 덮어씀)을 그대로 두지 않았다 → server가 `fs::copy`를 **golden 대조**로 교체, 덮어쓰기는 `STARFALL_REPLAY_BLESS=1`일 때만. **일부러 깨뜨려 실패하는 것까지 확인**했다.
  - 리더가 golden 3파일을 **인덱스에 스테이징**(커밋 아님). 이것만으로 `git diff --exit-code`가 drift를 잡는다 — 리더 실행 확인: determinism 테스트 후 **exit 0**.
- **SC-40(f) 권고 찬성**: JSON Schema 2020-12에서 `"type":"integer"`는 소수부 0인 수를 정수로 받으므로 `10.0`은 **스펙상 유효**하다. 스키마가 못 막는 건 버그가 아니라 정의이고 실제 거부 주체는 serde다 — **차이를 만드는 층이 그 차이를 단언해야 한다.** 조건: 검사 건수가 0이면 실패시킬 것.
- **라운드 1 판정 영향 없음** — `contracts/`를 건드리지 않아 계약 파일 수(18/26/34/13) 실측 기반이 유지된다.
- **라운드 2 신규 항목**: determinism 테스트 실행 후 `git diff --exit-code server/crates/sim/tests/data/replay/` = 0.

## 다음 슬라이스로 넘길 것 (p1-01에서 발견, 이번에 고치지 않음)
- `check_contract_coverage.py --strict`는 **타입 이름만 보고 열거 값은 보지 않는다** — Rust에 `SUPERSEDED`가 없어도 PASS였다(architect 실측 2026-09-22). 이번엔 Rust fixture 왕복 테스트가 잡았지만, 커버리지 검사가 열거 드리프트를 못 본다는 사실 자체가 "초록인데 아무것도 뜻하지 않는" 후보다.
- 계약 §3.3("QA 도구 자체 검증") 범위에 **측정 대상을 기동·종료시키는 드라이버**를 넣는다(qa 권고, architect 동의).
- `/debug/stats`에 **로그 싱크 버린 줄 수** 노출 — 키 수(31)가 바뀌는 첫 슬라이스에서 함께(architect 사안 6).
- 레지스트리 `responses` 필드(명령→응답 관계) — **세 번째 명령 타입이 들어오는 슬라이스**에서 다음 `registry_version` 인상과 함께 ADR로(architect R3 판정 2).
- 스펙 Q15: sim이 `reconnect_resume_window_seconds`를 쓰지 않는다 — 창 < 잔류가 되면 I-29가 깨진다(architect 부수 발견).
- DB의 자기 참조 `SHIP_DESPAWNED` 7건 — 역사 엔진의 **정정 메커니즘**이 생길 때 정정 레코드를 붙인다(지우지 않는다, 원칙 5).

## 다음 후보
- 이후 p1: 채굴 → 인벤토리 → 거래 → 기본 전투 → Historical Event 연결

## 운영 메모
- 구현 역할은 Sonnet 5 + TDD, 판단 역할은 Opus 5 (2026-09-19부터). 근거: `.claude/skills/starfall-dev/references/model-assignment.md`
- 에이전트 팀 도구(`SendMessage`)가 이 세션에서 동작함을 확인(2026-09-20).
- **긴 에이전트는 재개되지 않는다 (2026-09-20 실측).** 963K 토큰을 쓴 qa 서브 에이전트를 `SendMessage`로 재개하려 하자 `Prompt is too long (invalid_request)`로 즉시 종료됐다. 따라서 **한 에이전트에게 긴 평가를 통째로 맡기지 않는다** — 블록 단위로 범위를 잘라 투입하고, 맥락이 아니라 **파일**(`evidence/`, `04_qa_report_r{N}.md`)로 다음 에이전트에게 넘긴다. 에이전트에게도 "블록마다 즉시 디스크에 쓰라"고 지시한다.
