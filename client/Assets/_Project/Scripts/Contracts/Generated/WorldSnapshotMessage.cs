// <auto-generated>
//     Generated by tools/codegen/ContractsCodegen.cs from contracts/messages/WORLD_SNAPSHOT.schema.json.
//     Do not edit. Re-run the generator; `--check` fails on hand edits.
// </auto-generated>
#nullable disable
using Newtonsoft.Json;

namespace Starfall.Contracts.Generated
{
    /// <summary>WORLD_SNAPSHOT (schema_version 1). Generated from contracts/messages/WORLD_SNAPSHOT.schema.json; do not edit.</summary>
    public sealed class WorldSnapshotMessage
    {
        public const string MessageTypeConst = "WORLD_SNAPSHOT";

        public const int SchemaVersionConst = 1;

        /// <summary>Gameplay transaction this message belongs to, or null. Reply matching uses explicit payload fields (e.g. command_id), not this field.</summary>
        [JsonProperty("correlation_id", Required = Required.AllowNull)]
        public System.Guid? CorrelationId { get; set; }

        /// <summary>Server-generated UUIDv7 for tracing and log correlation.</summary>
        [JsonProperty("message_id", Required = Required.Always)]
        public System.Guid MessageId { get; set; }

        /// <summary>Registry type name in SCREAMING_SNAKE_CASE.</summary>
        [JsonProperty("message_type", Required = Required.Always)]
        public string MessageType { get; set; } = MessageTypeConst;

        [JsonProperty("payload", Required = Required.Always)]
        public WorldSnapshotPayload Payload { get; set; }

        /// <summary>Payload schema version of a registry type. Starts at 1. 매핑은 declared minimum/maximum으로 결정한다.</summary>
        [JsonProperty("schema_version", Required = Required.Always)]
        public int SchemaVersion { get; set; } = SchemaVersionConst;

        /// <summary>Server simulation tick when the message was produced.</summary>
        [JsonProperty("tick", Required = Required.Always)]
        public long Tick { get; set; }

        public sealed class WorldSnapshotPayload
        {
            /// <summary>Newest SET_SHIP_CONTROL input_seq the server had applied for this session as of this tick, or null if it has applied none. The client rebases on this ship state and replays every input after this value. Without it, prediction cannot be reconciled at all (ADR-0012 section 3).</summary>
            [JsonProperty("ack_input_seq", Required = Required.AllowNull)]
            public uint? AckInputSeq { get; set; }

            /// <summary>The ship this session's actor is flying, or null if it has none. It is the entry in ships the client must predict rather than interpolate.</summary>
            [JsonProperty("controlled_ship_id", Required = Required.AllowNull)]
            public System.Guid? ControlledShipId { get; set; }

            /// <summary>The wall. The server projects any ship that reaches it back onto this sphere and removes the outward radial velocity (I-34). The maximum is the float32 rendering budget of ADR-0009 section 3, not a design choice: a larger world needs a Floating Origin slice first.</summary>
            [JsonProperty("hard_boundary_radius_mm", Required = Required.Always)]
            public int HardBoundaryRadiusMm { get; set; }

            /// <summary>Every ship in the system, active and lingering, ordered by ship_id so the same world state always serialises to the same bytes (I-37).</summary>
            [JsonProperty("ships", Required = Required.Always)]
            public ShipState[] Ships { get; set; }

            /// <summary>How many ticks apart consecutive snapshots are. The client sizes its interpolation buffer from this instead of measuring arrival times (ADR-0012).</summary>
            [JsonProperty("snapshot_interval_ticks", Required = Required.Always)]
            public int SnapshotIntervalTicks { get; set; }

            /// <summary>Beyond this radius the server applies a constant pull back toward the origin. The client draws the boundary warning here. It is not a wall.</summary>
            [JsonProperty("soft_boundary_radius_mm", Required = Required.Always)]
            public int SoftBoundaryRadiusMm { get; set; }

            /// <summary>Star system these coordinates are local to. One system exists today; the field is here so a later system never has to be told apart by guessing.</summary>
            [JsonProperty("star_system_id", Required = Required.Always)]
            public string StarSystemId { get; set; }

            /// <summary>One ship's authoritative state, quantised (ADR-0009). Consumers renormalise the orientation before use. Angular velocity is TWO values, not one: the attitude controller's world-frame vector and the roll scalar. 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다.</summary>
            public sealed class ShipState
            {
                /// <summary>Actor this ship belongs to, decided by the server from the authenticated session. It stays set while the ship is LINGERING.</summary>
                [JsonProperty("actor_id", Required = Required.Always)]
                public System.Guid ActorId { get; set; }

                /// <summary>Roll rate about the ship-local +Z (forward) axis, the state of the manual-roll and auto-levelling controller. Carried as its own scalar because the attitude controller's angular velocity is not guaranteed to be perpendicular to the forward axis, so the roll rate cannot be recovered by projecting the vector above (ADR-0010 section 2).</summary>
                [JsonProperty("angular_velocity_roll_mdeg_s", Required = Required.Always)]
                public int AngularVelocityRollMdegS { get; set; }

                /// <summary>World-frame angular velocity of the ATTITUDE controller (omega_aim), x component. IT DOES NOT INCLUDE ROLL - roll is carried separately in angular_velocity_roll_mdeg_s. The two are separate state with separate slew limits and they are integrated as two successive first-order steps, so their sum is not enough to reproduce them: a consumer that split the sum would mis-attribute the roll component and diverge while turning.</summary>
                [JsonProperty("angular_velocity_x_mdeg_s", Required = Required.Always)]
                public int AngularVelocityXMdegS { get; set; }

                /// <summary>World-frame angular velocity of the attitude controller (omega_aim), y component. Roll not included.</summary>
                [JsonProperty("angular_velocity_y_mdeg_s", Required = Required.Always)]
                public int AngularVelocityYMdegS { get; set; }

                /// <summary>World-frame angular velocity of the attitude controller (omega_aim), z component. Roll not included.</summary>
                [JsonProperty("angular_velocity_z_mdeg_s", Required = Required.Always)]
                public int AngularVelocityZMdegS { get; set; }

                /// <summary>One component of a normalised quaternion, in millionths (1e-6). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The quantised quaternion is not exactly unit length; both producer and consumer renormalise before use, and a quaternion whose norm is below 1e-3 is degenerate and is replaced by the ship's current attitude rather than rejected (ADR-0010 section 2).</summary>
                [JsonProperty("orientation_w_micro", Required = Required.Always)]
                public int OrientationWMicro { get; set; }

                /// <summary>One component of a normalised quaternion, in millionths (1e-6). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The quantised quaternion is not exactly unit length; both producer and consumer renormalise before use, and a quaternion whose norm is below 1e-3 is degenerate and is replaced by the ship's current attitude rather than rejected (ADR-0010 section 2).</summary>
                [JsonProperty("orientation_x_micro", Required = Required.Always)]
                public int OrientationXMicro { get; set; }

                /// <summary>One component of a normalised quaternion, in millionths (1e-6). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The quantised quaternion is not exactly unit length; both producer and consumer renormalise before use, and a quaternion whose norm is below 1e-3 is degenerate and is replaced by the ship's current attitude rather than rejected (ADR-0010 section 2).</summary>
                [JsonProperty("orientation_y_micro", Required = Required.Always)]
                public int OrientationYMicro { get; set; }

                /// <summary>One component of a normalised quaternion, in millionths (1e-6). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The quantised quaternion is not exactly unit length; both producer and consumer renormalise before use, and a quaternion whose norm is below 1e-3 is degenerate and is replaced by the ship's current attitude rather than rejected (ADR-0010 section 2).</summary>
                [JsonProperty("orientation_z_micro", Required = Required.Always)]
                public int OrientationZMicro { get; set; }

                /// <summary>One axis of a star-system-local position, in integer millimetres (ADR-0009). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The wire carries no floating point: the server holds f64 metres and quantises once, so every consumer sees the same bytes. Range is structural headroom (+/-1e12 mm), NOT the play area - that limit is a game rule in the star system data table.</summary>
                [JsonProperty("position_x_mm", Required = Required.Always)]
                public long PositionXMm { get; set; }

                /// <summary>One axis of a star-system-local position, in integer millimetres (ADR-0009). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The wire carries no floating point: the server holds f64 metres and quantises once, so every consumer sees the same bytes. Range is structural headroom (+/-1e12 mm), NOT the play area - that limit is a game rule in the star system data table.</summary>
                [JsonProperty("position_y_mm", Required = Required.Always)]
                public long PositionYMm { get; set; }

                /// <summary>One axis of a star-system-local position, in integer millimetres (ADR-0009). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. The wire carries no floating point: the server holds f64 metres and quantises once, so every consumer sees the same bytes. Range is structural headroom (+/-1e12 mm), NOT the play area - that limit is a game rule in the star system data table.</summary>
                [JsonProperty("position_z_mm", Required = Required.Always)]
                public long PositionZMm { get; set; }

                /// <summary>ACTIVE means a live session is flying it. LINGERING means the session ended and the ship is coasting until its linger window expires - still visible to everyone, still solid world state, and reclaimed intact if the same actor reconnects in time. A ship that vanishes the instant its pilot disconnects is the exact opposite of a world that remembers.</summary>
                [JsonProperty("presence", Required = Required.Always)]
                public string Presence { get; set; }

                /// <summary>Row of the SHIP_CLASS data table this ship was built from. It fixes the acceleration, speed and turn limits the server applied, so the client predicts with the same numbers.</summary>
                [JsonProperty("ship_class_id", Required = Required.Always)]
                public string ShipClassId { get; set; }

                /// <summary>Identity of the ship entity. A ship is not its pilot (GDD section 4): this id, not actor_id, is what later slices attach ship history to, and it survives a reconnect inside the linger window.</summary>
                [JsonProperty("ship_id", Required = Required.Always)]
                public System.Guid ShipId { get; set; }

                /// <summary>Required for remote extrapolation: without it a late snapshot makes every other ship stall and then jump.</summary>
                [JsonProperty("velocity_x_mm_s", Required = Required.Always)]
                public int VelocityXMmS { get; set; }

                /// <summary>One axis of a velocity, in integer millimetres per second (ADR-0009). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. Range is structural headroom (+/-100 km/s); the ship class table sets the real maximum speed.</summary>
                [JsonProperty("velocity_y_mm_s", Required = Required.Always)]
                public int VelocityYMmS { get; set; }

                /// <summary>One axis of a velocity, in integer millimetres per second (ADR-0009). 좌표계 규약 (ADR-0009 section 1): 왼손 좌표계, Y-up. +X = 함선 기준 오른쪽, +Y = 위, +Z = 전방(뱃머리). 쿼터니언은 (x,y,z,w) 순서의 Hamilton 곱. 이 문장이 축 의미의 정본이며 구현은 여기서만 읽는다. Range is structural headroom (+/-100 km/s); the ship class table sets the real maximum speed.</summary>
                [JsonProperty("velocity_z_mm_s", Required = Required.Always)]
                public int VelocityZMmS { get; set; }
            }
        }
    }
}
