//! `sqlx::migrate!` 의 재컴파일 트리거.
//!
//! server 실측(2026-09-18): sqlx 0.8.6 에서 **기존 마이그레이션 파일을 수정하면** 재컴파일되지만
//! **새 파일을 추가하면 재컴파일되지 않는다** — 새 마이그레이션이 바이너리에 들어가지 않은 채
//! "마이그레이션을 썼는데 테이블이 없다"가 된다. 디렉토리를 추적하면 두 경우 모두 잡힌다
//! (같은 실측에서 확인: 변화가 없을 때 불필요한 재빌드도 일으키지 않는다).
//!
//! ADR-0007 §5 의 "마이그레이션 파일만 바뀌었을 때"는 정확히는 "**추가**했을 때"다.
fn main() {
    println!("cargo:rerun-if-changed=../../migrations");
}
